  CREATE TABLE "TASA_CAMBIO_USD_VES" 
   (	"ID_TASA" NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY MINVALUE 1 MAXVALUE 9999999999999999999999999999 INCREMENT BY 1 START WITH 1 CACHE 20 NOORDER  NOCYCLE  NOKEEP  NOSCALE  NOT NULL ENABLE, 
	"FECHA" DATE, 
	"TASA" NUMBER, 
	"CBY" VARCHAR2(50), 
	"CDT" TIMESTAMP (6), 
	"MBY" VARCHAR2(50), 
	"MDT" TIMESTAMP (6), 
	"ORIGEN" VARCHAR2(1), 
	 PRIMARY KEY ("ID_TASA")
  USING INDEX  ENABLE
   ) ;

  CREATE OR REPLACE EDITIONABLE TRIGGER "TRG_TASA_CAMBIO_USD_VES_AUDIT" 
BEFORE INSERT OR UPDATE ON TASA_CAMBIO_USD_VES 
FOR EACH ROW 
BEGIN 
    :NEW.MDT := SYSTIMESTAMP; 
    :NEW.MBY := NVL(v('APP_USER'), USER); 
    IF INSERTING THEN 
        :NEW.CDT := SYSTIMESTAMP; 
        :NEW.CBY := NVL(v('APP_USER'), USER); 
    END IF; 
END;
/
ALTER TRIGGER "TRG_TASA_CAMBIO_USD_VES_AUDIT" ENABLE;