  CREATE TABLE "AGENCIA" 
   (	"ID_AGENCIA" NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY MINVALUE 1 MAXVALUE 9999999999999999999999999999 INCREMENT BY 1 START WITH 1 CACHE 20 NOORDER  NOCYCLE  NOKEEP  NOSCALE  NOT NULL ENABLE, 
	"NOMBRE" VARCHAR2(100) NOT NULL ENABLE, 
	"DIRECCION" VARCHAR2(255), 
	"TELEFONO" VARCHAR2(20), 
	"DESCRIPCION" VARCHAR2(255), 
	"CBY" VARCHAR2(50), 
	"CDT" TIMESTAMP (6), 
	"MBY" VARCHAR2(50), 
	"MDT" TIMESTAMP (6), 
	 PRIMARY KEY ("ID_AGENCIA")
  USING INDEX  ENABLE, 
	 CONSTRAINT "AGENCIA_UK01" UNIQUE ("NOMBRE")
  USING INDEX  ENABLE
   ) ;

  CREATE OR REPLACE EDITIONABLE TRIGGER "TRG_AGENCIA_AUDIT" 
BEFORE INSERT OR UPDATE ON AGENCIA 
FOR EACH ROW 
BEGIN 
    :NEW.MDT := SYSTIMESTAMP; 
    :NEW.MBY := NVL(v('APP_USER'), USER); 
    IF INSERTING THEN 
        :NEW.CDT := SYSTIMESTAMP; 
        :NEW.CBY := NVL(v('APP_USER'), USER); 
    END IF; 
END;
/
ALTER TRIGGER "TRG_AGENCIA_AUDIT" ENABLE;