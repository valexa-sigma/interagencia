  CREATE TABLE "CARDS" 
   (	"ID_CARD" NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY MINVALUE 1 MAXVALUE 9999999999999999999999999999 INCREMENT BY 1 START WITH 1 CACHE 20 NOORDER  NOCYCLE  NOKEEP  NOSCALE  NOT NULL ENABLE, 
	"CODE" VARCHAR2(20), 
	"TITLE" VARCHAR2(40), 
	"SUBTITLE" VARCHAR2(64), 
	"BODY" VARCHAR2(255), 
	"SUBBODY" VARCHAR2(255), 
	"CATEGORY" VARCHAR2(20), 
	"ICON" VARCHAR2(30), 
	"BADGE" VARCHAR2(30), 
	"IMAGE" BLOB, 
	"SEQ" NUMBER, 
	"CBY" VARCHAR2(30), 
	"CDT" TIMESTAMP (6), 
	"MBY" VARCHAR2(30), 
	"MDT" TIMESTAMP (6), 
	"TARGET_PAGE" NUMBER, 
	"ACTIVO" VARCHAR2(1), 
	 PRIMARY KEY ("ID_CARD")
  USING INDEX  ENABLE
   ) ;

  CREATE OR REPLACE EDITIONABLE TRIGGER "TRG_CARDS" 
BEFORE INSERT OR UPDATE ON CARDS 
FOR EACH ROW 
BEGIN 
    :NEW.MDT := SYSTIMESTAMP; 
    :NEW.MBY := NVL(v('APP_USER'), USER); 
    IF INSERTING THEN 
        :NEW.CDT := SYSTIMESTAMP; 
        :NEW.CBY := NVL(v('APP_USER'), USER); 
    END IF; 
END;
/
ALTER TRIGGER "TRG_CARDS" ENABLE;